# EECS 182 - Fall 2026 HW02 coding materials

Use the notebook linked for your problem in the homework PDF:

- [Initialization and Optimizers](code/hw2_optimizer_init.ipynb): run the supplied implementations and experiments, then include the requested observations and plots in your written PDF. Keep the accompanying `deeplearning` package and `assignment_utils.py` in the same folder; optional experiment records are saved in `experiment_logs`.
- [Local Linearization](q_coding_linearized_features/code/q_linearized_features.ipynb).
- [Optimization of Difficult Objectives](q_coding_rl/code/opt.ipynb).
- [ReLU with Different Optimizers](q_relu_optim/code/q_relu_optim.ipynb): the helpers and supplied checkpoints are included in its own HW02 folder.

The Local Linearization and Optimization of Difficult Objectives notebooks contain student TODOs. Complete them before running their associated experiments. The ReLU optimizer notebook uses supplied checkpoints; its deliberately faulty loop is a written debugging exercise, not an executable cell.
